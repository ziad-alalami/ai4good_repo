

Install uv with 
```bash 
pip install uv
```

and 

```bash
uv sync
```

to install venv dependencies

and 

```bash
uv add <dependency_name>
```
to add a dependency

```bash
uv run python ....
```
to run using pythong